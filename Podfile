# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'TestAssign' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!
  pod 'p2.OAuth2'
  pod 'SDWebImage', '~> 4.0'

  # Pods for TestAssign

  target 'TestAssignTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'TestAssignUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
